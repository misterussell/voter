import Backbone from 'backbone';

export default Backbone.Model.extend({
  idAttribute: 'id',
  // parse (data) {
  //   // console.log(data);
  //   let bands = data.data.map((band) => {
  //     return band;
  //   });
  //   console.log(bands);
  //   return bands;
  // }
});
